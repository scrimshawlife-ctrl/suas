diff --git a/node_modules/find-my-way/lib/constrainer.js b/node_modules/find-my-way/lib/constrainer.js
--- a/node_modules/find-my-way/lib/constrainer.js
+++ b/node_modules/find-my-way/lib/constrainer.js
@@ -169,7 +169,17 @@
 
     lines.push('}')
 
-    this.deriveSyncConstraints = new Function('req', 'ctx', lines.join('\n')).bind(this) // eslint-disable-line
+    if (lines.length <= 2) {
+      // No derived constraints: Workers-safe no-op.
+      this.deriveSyncConstraints = () => undefined
+    } else {
+      try {
+        this.deriveSyncConstraints = new Function('req', 'ctx', lines.join('\n')).bind(this) // eslint-disable-line
+      } catch (err) {
+        // Edge runtimes that forbid codegen: fall back to empty constraints.
+        this.deriveSyncConstraints = () => undefined
+      }
+    }
   }
 }
 
diff --git a/node_modules/find-my-way/lib/handler-storage.js b/node_modules/find-my-way/lib/handler-storage.js
--- a/node_modules/find-my-way/lib/handler-storage.js
+++ b/node_modules/find-my-way/lib/handler-storage.js
@@ -75,7 +75,12 @@
 
     fnBody.push('return fn')
 
-    return new Function('NullObject', fnBody.join('\n'))(NullObject)  // eslint-disable-line
+    try {
+      return new Function('NullObject', fnBody.join('\n'))(NullObject)  // eslint-disable-line
+    } catch (err) {
+      // Workers forbid codegen; fall back to linear constraint matching.
+      return function noCompiledHandlers () { return null }
+    }
   }
 
   _getHandlerMatchingConstraints () {
@@ -168,7 +173,11 @@
     // Return the highest set bit index in the candidates bitmask.
     lines.push('return this.handlers[31 - Math.clz32(candidates)]')
 
-    this._getHandlerMatchingConstraints = new Function('derivedConstraints', lines.join('\n')) // eslint-disable-line
+    try {
+      this._getHandlerMatchingConstraints = new Function('derivedConstraints', lines.join('\n')) // eslint-disable-line
+    } catch (err) {
+      this._getHandlerMatchingConstraints = () => null
+    }
   }
 }
 
diff --git a/node_modules/find-my-way/lib/node.js b/node_modules/find-my-way/lib/node.js
--- a/node_modules/find-my-way/lib/node.js
+++ b/node_modules/find-my-way/lib/node.js
@@ -212,12 +212,14 @@
       return
     }
 
-    const lines = []
-    for (let i = 1; i < this.prefix.length; i++) {
-      const charCode = this.prefix.charCodeAt(i)
-      lines.push(`path.charCodeAt(i + ${i}) === ${charCode}`)
+    // Cloudflare Workers disallow `new Function`. Use a closed-over loop instead.
+    const prefix = this.prefix
+    this.matchPrefix = (path, i) => {
+      for (let j = 1; j < prefix.length; j++) {
+        if (path.charCodeAt(i + j) !== prefix.charCodeAt(j)) return false
+      }
+      return true
     }
-    this.matchPrefix = new Function('path', 'i', `return ${lines.join(' && ')}`) // eslint-disable-line
   }
 }
 
